#include <bits/stdc++.h>

using namespace std;

int long long n, k;

int main()
{
    cin >> n;
    
    while(n % 2 == 0)
    {
        n = n >> 1;
        k ++;
    }
    
    if(n == 1)
    {
        cout << k;
    }
    else
    {
        cout << -1;   
    }
    

    return 0;
}
